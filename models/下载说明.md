# 特征提取器模型下载说明

## 📁 文件夹结构

模型需要下载到以下目录：

```
quantum_multimodal_comparison/
└── models/
    ├── bert-base-uncased/              # BERT 文本特征提取器
    │   ├── config.json                 # ✅ 必需
    │   ├── pytorch_model.bin           # ✅ 必需（或 model.safetensors）
    │   ├── tokenizer_config.json      # ✅ 必需
    │   ├── vocab.txt                   # ✅ 必需
    │   ├── tokenizer.json              # ✅ 必需
    │   └── (其他文件)
    └── google-vit-base-patch16-224/    # ViT 图像特征提取器
        ├── config.json                 # ✅ 必需
        ├── pytorch_model.bin           # ✅ 必需（或 model.safetensors）
        └── (其他文件)
```

## 📥 下载方法

### 方法1: 使用 Python 脚本（推荐，支持镜像）

```bash
# 进入项目目录
cd quantum_multimodal_comparison

# 运行下载脚本（自动使用镜像站点）
python utils/download_models_guide.py

# 或者不使用镜像
python utils/download_models_guide.py --no-mirror
```

**注意**: 如果网络连接有问题，脚本会自动使用镜像站点 `https://hf-mirror.com`

### 方法2: 使用 Python 代码手动下载

创建并运行以下脚本：

```python
from huggingface_hub import snapshot_download
import os

# 设置镜像（中国用户推荐，解决连接超时问题）
os.environ['HF_ENDPOINT'] = 'https://hf-mirror.com'

# 下载 BERT
print("下载 BERT 模型...")
snapshot_download(
    repo_id="bert-base-uncased",
    local_dir="models/bert-base-uncased",
    resume_download=True  # 支持断点续传
)

# 下载 ViT
print("下载 ViT 模型...")
snapshot_download(
    repo_id="google/vit-base-patch16-224",
    local_dir="models/google-vit-base-patch16-224",
    resume_download=True  # 支持断点续传
)

print("下载完成！")
```

**重要**: 如果遇到连接超时，必须设置镜像：
```python
os.environ['HF_ENDPOINT'] = 'https://hf-mirror.com'
```

### 方法3: 手动从网页下载（推荐用于网络问题）

如果自动下载失败，可以手动下载：

1. **访问镜像站点**（推荐，速度快）：
   - BERT: https://hf-mirror.com/models/bert-base-uncased/tree/main
   - ViT: https://hf-mirror.com/models/google/vit-base-patch16-224/tree/main

2. **或访问官方站点**：
   - BERT: https://huggingface.co/bert-base-uncased/tree/main
   - ViT: https://huggingface.co/google/vit-base-patch16-224/tree/main

3. **下载必需文件**到对应文件夹：
   - `models/bert-base-uncased/` - 下载 BERT 的所有必需文件
   - `models/google-vit-base-patch16-224/` - 下载 ViT 的所有必需文件

4. **下载完成后运行测试**：
   ```bash
   python utils/test_feature_extractors.py
   ```

## 📋 必需文件清单

### BERT 模型 (`models/bert-base-uncased/`)

**必需文件（5个）：**
- ✅ `config.json` - 模型配置
- ✅ `pytorch_model.bin` 或 `model.safetensors` - 模型权重
- ✅ `tokenizer_config.json` - Tokenizer 配置
- ✅ `vocab.txt` - 词汇表
- ✅ `tokenizer.json` - Tokenizer 文件

### ViT 模型 (`models/google-vit-base-patch16-224/`)

**必需文件（2个）：**
- ✅ `config.json` - 模型配置
- ✅ `pytorch_model.bin` 或 `model.safetensors` - 模型权重

## 📊 文件大小

- **BERT**: 约 440 MB
- **ViT**: 约 330 MB
- **总计**: 约 770 MB

## ✅ 验证下载

下载完成后，运行测试脚本：

```bash
cd quantum_multimodal_comparison
python utils/test_feature_extractors.py --model-dir models
```

如果所有测试通过，说明模型下载正确！

## ⚠️ 注意事项

1. **路径格式**: ViT 模型的路径中 `/` 需要替换为 `-`
   - 模型ID: `google/vit-base-patch16-224`
   - 本地路径: `models/google-vit-base-patch16-224`

2. **文件格式**: 
   - 优先使用 `model.safetensors`（更安全、更快）
   - 如果没有则使用 `pytorch_model.bin`

3. **磁盘空间**: 确保有至少 1GB 的可用空间

4. **网络**: 如果下载慢，可以使用镜像站点：
   ```bash
   export HF_ENDPOINT=https://hf-mirror.com  # Linux/Mac
   $env:HF_ENDPOINT='https://hf-mirror.com'  # Windows PowerShell
   ```

5. **权限**: 确保有写入权限创建文件夹和文件

## 🔧 故障排除

### 问题1: 下载失败

**解决方案:**
- 检查网络连接
- 使用镜像站点
- 检查 HuggingFace 访问权限

### 问题2: 文件不完整

**解决方案:**
- 重新下载缺失的文件
- 检查文件大小是否正常

### 问题3: 测试失败

**解决方案:**
- 检查所有必需文件是否存在
- 检查文件路径是否正确
- 查看测试脚本的错误信息
