# 快速下载指南（解决网络问题）

## 🚀 最简单的方法

直接运行脚本，会自动使用镜像站点：

```bash
cd quantum_multimodal_comparison
python utils/download_models_guide.py
```

## 🔧 如果还是失败

### 方法1: 手动设置镜像环境变量

**Windows PowerShell:**
```powershell
$env:HF_ENDPOINT='https://hf-mirror.com'
python utils/download_models_guide.py
```

**Windows CMD:**
```cmd
set HF_ENDPOINT=https://hf-mirror.com
python utils/download_models_guide.py
```

**Linux/Mac:**
```bash
export HF_ENDPOINT=https://hf-mirror.com
python utils/download_models_guide.py
```

### 方法2: 使用 Python 代码（最可靠）

创建文件 `download_manual.py`:

```python
from huggingface_hub import snapshot_download
import os
from pathlib import Path

# 强制使用镜像
os.environ['HF_ENDPOINT'] = 'https://hf-mirror.com'

# 项目根目录
base_dir = Path(__file__).parent
models_dir = base_dir / "models"

print("开始下载模型...")
print(f"保存到: {models_dir}")

# 下载 BERT
print("\n1. 下载 BERT...")
try:
    snapshot_download(
        repo_id="bert-base-uncased",
        local_dir=str(models_dir / "bert-base-uncased"),
        local_dir_use_symlinks=False,
        resume_download=True
    )
    print("✓ BERT 完成")
except Exception as e:
    print(f"✗ BERT 失败: {e}")

# 下载 ViT
print("\n2. 下载 ViT...")
try:
    snapshot_download(
        repo_id="google/vit-base-patch16-224",
        local_dir=str(models_dir / "google-vit-base-patch16-224"),
        local_dir_use_symlinks=False,
        resume_download=True
    )
    print("✓ ViT 完成")
except Exception as e:
    print(f"✗ ViT 失败: {e}")

print("\n完成！运行测试: python utils/test_feature_extractors.py")
```

然后运行：
```bash
python download_manual.py
```

### 方法3: 手动网页下载

如果所有自动方法都失败，手动下载：

1. 访问镜像站点：
   - BERT: https://hf-mirror.com/models/bert-base-uncased/tree/main
   - ViT: https://hf-mirror.com/models/google/vit-base-patch16-224/tree/main

2. 点击每个文件下载到对应文件夹

3. 运行测试验证

## ✅ 验证下载

下载完成后：
```bash
python utils/test_feature_extractors.py
```
